# TASK-HUB-012: 要求・トレーサビリティのデータ抽出

**要件**: REQ-023, REQ-024

## 受入条件

- J-SIX の `docs/requirement-spec.md` をコミット時点で読み、要件（REQ）と性質（PROP）の一覧をイベントにする
- `docs/traceability.md` をコミット時点で読み、要件 ⇔ テスト ⇔ 実装（⇔ ADR）の対応をイベントにする
- 追加された要件が分かるよう、前回からの差分（追加 ID）をイベントに持たせる
- 架空の案件にも、同じ形式の要求・トレーサビリティを再構成として用意する
- 生成したデータがスキーマに適合し、公開してはいけない情報を含まない（既存の検査で確認）

## 変更許可範囲（allow）

- `tools/**`
- `data/**`
- `docs/**`

## 変更禁止（deny）

- `LICENSE`

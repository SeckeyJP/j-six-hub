# TASK-HUB-017: デザインレビュー②-1：用語と文言の統一

**要件**: REQ-027

**出どころ**: デザインレビュー2回目の指摘（受領物は git 管理対象外）

## 受入条件

- 画面に出る文から「案件」が無くなる（再構成イベントの文も含む。プロセス定義由来の名前は変えない）
- 工程ボードの導入文・見出しが、画面の用語（品質検査・例外処理）に揃う
- トップバーに、このシステムが何かを1行で示す
- ガイドの文が、いまの画面の表示（停止 n）と一致する
- 順序違反の文が「P1 の承認前に P2 が始まった」の形になる

## 変更許可範囲（allow）

- `src/**`
- `src/styles/**`
- `data/projects.json`
- `data/projects/*/reconstructed.json`（生成物 `events.jsonl` は抽出スクリプトで作り直す）
- `docs/**`

## 変更禁止（deny）

- `data/projects/*/events.jsonl` の直接編集
- `tools/extract_events.py` の抽出規則（文言の置き換え以外）
- `LICENSE`

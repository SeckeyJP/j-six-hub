# TASK-HUB-005: 画面：タスク・ゲート・承認・証跡

**要件**: REQ-007, 008, 009, 012

## 受入条件

- タスクの状態・工程・サブエージェントの履歴を表示する
- ゲートの判定を層・チェックごとに表示し、繰り返し回数を示す
- 無効な承認を理由とともに区別して表示する
- 証跡パッケージの生成結果と J-SIX のファイルへのリンクを表示する

## 変更許可範囲（allow）

- `src/ui/**`
- `src/styles/**`

## 変更禁止（deny）

- `data/**`（イベントは抽出スクリプトの生成物）
- `LICENSE`
